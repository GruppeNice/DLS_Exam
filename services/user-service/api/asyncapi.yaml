asyncapi: 2.6.0
info:
  title: User Service Events
  version: 1.0.0
channels:
  user.registered:
    publish:
      message:
        name: UserRegistered
  user.suspended:
    publish:
      message:
        name: UserSuspended
  user.deleted:
    publish:
      message:
        name: UserDeleted

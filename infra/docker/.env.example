# Copy to .env and fill in Google OAuth credentials (optional).
# Used by docker-compose for user-service (see env_file in docker-compose.yml).

GOOGLE_OAUTH_ENABLED=false
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_FRONTEND_REDIRECT=http://localhost:3000/oauth/callback
